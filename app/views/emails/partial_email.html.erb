<%= form_for email do |f| %>
  <%= f.check_box :read %>
  <%= f.submit "Update" %>
  <%= f.label :read, email.name %>
  <%= link_to "(remove)", email, method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>
